query GetCharactersPage($page: Int) {
    characters(page: $page) {
        info {
            next
        }
        results {
            id
            name
            species
            type
            gender
            origin {
                name
            }
            image
        }
    }
}
